/**
 * Group Anagrams (Medium)
 *
 * A content moderation system needs to group product listings that are
 * anagram variants of each other (e.g. "eat", "tea", "ate" are the same
 * group). Given a list of product tags, group them by anagram.
 *
 * Pattern: Hash Map — sorted string as key, O(n * k log k)
 *
 * @param {string[]} strs - array of strings
 * @returns {string[][]} array of grouped anagrams
 */
export function groupAnagrams(strs) {
  // your implementation here
}

/**
 * Top K Frequent Elements (Medium)
 *
 * An analytics dashboard needs to surface the top K most-mentioned feature
 * requests from customer feedback. Given an array of feature IDs and a
 * number k, return the k most frequently occurring IDs.
 *
 * Pattern: Frequency Map + Bucket Sort, O(n) time
 *
 * @param {number[]} nums - array of IDs
 * @param {number} k - how many top elements to return
 * @returns {number[]} the k most frequent elements
 */
export function topKFrequent(nums, k) {
  // your implementation here
}

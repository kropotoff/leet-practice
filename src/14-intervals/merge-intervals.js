/**
 * Merge Intervals (Medium)
 *
 * A calendar scheduling tool merges overlapping meeting time slots into
 * consolidated blocks. Given a list of intervals [start, end], merge
 * all overlapping intervals and return the result.
 *
 * Pattern: Sort by start, merge overlapping, O(n log n) time
 *
 * @param {number[][]} intervals
 * @return {number[][]}
 */
export function merge(intervals) {
  // your implementation here
}

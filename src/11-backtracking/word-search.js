/**
 * Word Search (Medium)
 *
 * A crossword validator checks if a word can be formed by tracing
 * adjacent cells (horizontal/vertical) on a letter board, without
 * reusing any cell. Given an m x n board and a word, return true if
 * the word exists in the grid.
 *
 * Pattern: Grid DFS backtracking — mark visited cells
 *
 * @param {string[][]} board
 * @param {string} word
 * @return {boolean}
 */
export function exist(board, word) {
  // your implementation here
}

/**
 * Subsets (Medium)
 *
 * A marketing tool generates every possible combination of feature flags
 * for A/B testing. Given an array of unique feature IDs, return the
 * power set (all possible subsets, including empty and full).
 *
 * Pattern: Backtracking — include/exclude at each element
 *
 * @param {number[]} nums
 * @return {number[][]}
 */
export function subsets(nums) {
  // your implementation here
}

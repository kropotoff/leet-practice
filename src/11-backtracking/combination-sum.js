/**
 * Combination Sum (Medium)
 *
 * A budgeting tool finds all unique ways to allocate spending across
 * reusable budget categories to hit a target amount. The same category
 * can be used unlimited times. Given distinct candidate amounts and a
 * target, return all unique combinations that sum to the target.
 *
 * Pattern: Backtracking with element reuse
 *
 * @param {number[]} candidates
 * @param {number} target
 * @return {number[][]}
 */
export function combinationSum(candidates, target) {
  // your implementation here
}

/**
 * Implement Trie / Prefix Tree (Medium)
 *
 * An autocomplete engine stores and retrieves words using a prefix tree.
 * Implement a Trie class with:
 *   - insert(word)        — adds a word
 *   - search(word)        — returns true if exact word exists
 *   - startsWith(prefix)  — returns true if any word starts with prefix
 *
 * Pattern: Trie with children map and isEnd flag, O(L) per operation
 */
export class Trie {
  constructor() {
    // your initialization here
  }

  /** @param {string} word */
  insert(word) {
    // your implementation here
  }

  /**
   * @param {string} word
   * @return {boolean}
   */
  search(word) {
    // your implementation here
  }

  /**
   * @param {string} prefix
   * @return {boolean}
   */
  startsWith(prefix) {
    // your implementation here
  }
}

/**
 * Coin Change (Medium)
 *
 * A vending machine controller computes the minimum number of coins
 * needed to dispense a given change amount. Given an array of coin
 * denominations and a total amount, return the fewest coins needed.
 * Return -1 if exact change is impossible.
 *
 * Pattern: Bottom-up 1D DP — dp[i] = min coins for amount i
 *
 * @param {number[]} coins
 * @param {number} amount
 * @return {number}
 */
export function coinChange(coins, amount) {
  // your implementation here
}

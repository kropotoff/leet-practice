/**
 * Climbing Stairs (Easy)
 *
 * A game designer counts how many distinct paths a player can take to
 * reach level n, if each move advances 1 or 2 levels. Return the number
 * of distinct ways to reach level n.
 *
 * Pattern: Bottom-up DP (Fibonacci), O(n) time, O(1) space
 *
 * @param {number} n
 * @return {number}
 */
export function climbStairs(n) {
  // your implementation here
}

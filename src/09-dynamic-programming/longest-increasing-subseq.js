/**
 * Longest Increasing Subsequence (Medium)
 *
 * A career-tracking tool finds the longest sequence of strictly
 * increasing performance scores in an employee's review history.
 * Given an integer array, return the length of the longest strictly
 * increasing subsequence.
 *
 * Pattern: Patience sort with binary search, O(n log n) time
 *
 * @param {number[]} nums
 * @return {number}
 */
export function lengthOfLIS(nums) {
  // your implementation here
}

/**
 * Min Stack (Medium)
 *
 * A real-time monitoring system tracks server metrics and must always
 * know the current minimum value in constant time, even as values are
 * pushed and popped. Design a stack that supports push, pop, top, and
 * getMin — all in O(1).
 *
 * Pattern: Two stacks — value stack + min-tracking stack
 */
export class MinStack {
  constructor() {
    // your initialization here
  }

  /** @param {number} val */
  push(val) {
    // your implementation here
  }

  pop() {
    // your implementation here
  }

  /** @return {number} */
  top() {
    // your implementation here
  }

  /** @return {number} */
  getMin() {
    // your implementation here
  }
}

/**
 * Valid Parentheses (Easy)
 *
 * A code linter validates that all brackets in a configuration file are
 * properly nested and matched. Given a string containing only '(', ')',
 * '{', '}', '[' and ']', return true if the nesting is valid.
 *
 * Pattern: Stack matching, O(n) time
 *
 * @param {string} s
 * @return {boolean}
 */
export function isValid(s) {
  // your implementation here
}

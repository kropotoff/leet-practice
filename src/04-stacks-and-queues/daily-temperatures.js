/**
 * Daily Temperatures (Medium)
 *
 * A retail forecast system tells store managers how many days they must
 * wait before a warmer day arrives (for seasonal inventory planning).
 * Given an array of daily temperatures, return an array where each
 * element is the number of days until a warmer temperature. If no warmer
 * day exists, use 0.
 *
 * Pattern: Monotonic decreasing stack, O(n) time
 *
 * @param {number[]} temperatures
 * @return {number[]}
 */
export function dailyTemperatures(temperatures) {
  // your implementation here
}

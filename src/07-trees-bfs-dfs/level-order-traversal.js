/**
 * Binary Tree Level Order Traversal (Medium)
 *
 * An org chart report lists employees level by level — CEO first, then
 * VPs, then directors, etc. Given the root of a binary tree, return
 * its values grouped by level (left to right within each level).
 *
 * Pattern: BFS with queue, O(n) time, O(n) space
 *
 * @param {TreeNode|null} root
 * @return {number[][]}
 */
import { TreeNode, arrayToTree } from './max-depth-binary-tree.js';

export { TreeNode, arrayToTree };

export function levelOrder(root) {
  // your implementation here
}

/**
 * Linked List Cycle (Easy)
 *
 * A dependency tracker needs to detect if a chain of package dependencies
 * forms a circular reference. Given the head of a linked list, return
 * true if a cycle exists.
 *
 * Pattern: Floyd's tortoise and hare — fast/slow pointers, O(n) time, O(1) space
 *
 * @param {ListNode|null} head
 * @return {boolean}
 */
import { ListNode } from './reverse-linked-list.js';

export { ListNode };

export function hasCycle(head) {
  // your implementation here
}

/**
 * Merge Two Sorted Lists (Easy)
 *
 * Two departments each have a sorted task list by priority. Merge them
 * into a single sorted list by splicing the nodes together.
 *
 * Pattern: Dummy head node + comparison, O(n + m) time, O(1) space
 *
 * @param {ListNode|null} list1
 * @param {ListNode|null} list2
 * @return {ListNode|null}
 */
import { ListNode } from './reverse-linked-list.js';

export function mergeTwoLists(list1, list2) {
  // your implementation here
}

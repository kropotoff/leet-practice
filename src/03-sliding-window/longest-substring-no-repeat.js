/**
 * Longest Substring Without Repeating Characters (Medium)
 *
 * A username validator needs to find the longest stretch of unique
 * characters in a proposed username. Given a string, return the length
 * of the longest substring without repeating characters.
 *
 * Pattern: Sliding Window with a Set, O(n) time
 *
 * @param {string} s
 * @returns {number} length of longest non-repeating substring
 */
export function lengthOfLongestSubstring(s) {
  // your implementation here
}

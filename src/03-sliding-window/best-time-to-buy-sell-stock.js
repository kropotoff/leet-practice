/**
 * Best Time to Buy and Sell Stock (Easy)
 *
 * A trading algorithm finds the optimal single buy-and-sell window to
 * maximize profit. Given an array of daily stock prices, return the
 * maximum profit from one purchase and one sale. Return 0 if no profit
 * is possible.
 *
 * Pattern: Sliding Window — track min price, compute max profit, O(n)
 *
 * @param {number[]} prices - daily prices
 * @returns {number} maximum profit (0 if none)
 */
export function maxProfit(prices) {
  // your implementation here
}

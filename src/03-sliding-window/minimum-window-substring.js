/**
 * Minimum Window Substring (Hard)
 *
 * A search engine highlights the smallest passage in a document that
 * contains all required keyword characters. Given a source string s and
 * a target string t, return the minimum window in s that contains every
 * character of t (including duplicates). Return "" if no such window.
 *
 * Pattern: Sliding Window with frequency maps — expand right, shrink left
 *
 * @param {string} s - source string
 * @param {string} t - target characters
 * @returns {string} minimum window or ""
 */
export function minWindow(s, t) {
  // your implementation here
}

/**
 * Container With Most Water (Medium)
 *
 * A warehouse design tool calculates the maximum storage volume between
 * two partition walls. Given an array of wall heights, find the two walls
 * that together hold the most water (area = min height * distance).
 *
 * Pattern: Two Pointers — move the shorter side inward, O(n) time
 *
 * @param {number[]} height - array of wall heights
 * @returns {number} maximum water area
 */
export function maxArea(height) {
  // your implementation here
}

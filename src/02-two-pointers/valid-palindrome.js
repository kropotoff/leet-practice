/**
 * Valid Palindrome (Easy)
 *
 * A QA tool validates that serial codes read the same forwards and
 * backwards, ignoring non-alphanumeric characters and case. Given a
 * serial string, return true if it's a valid palindrome.
 *
 * Pattern: Two Pointers — inward sweep, O(n) time
 *
 * @param {string} s
 * @returns {boolean}
 */
export function isPalindrome(s) {
  // your implementation here
}

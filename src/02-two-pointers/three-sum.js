/**
 * 3Sum (Medium)
 *
 * An accounting system needs to find all unique sets of three transactions
 * that net to zero (debits and credits cancel out). Given an array of
 * transaction amounts, return all unique triplets [a, b, c] where
 * a + b + c === 0.
 *
 * Pattern: Sort + Two Pointers — skip duplicates, O(n^2) time
 *
 * @param {number[]} nums
 * @returns {number[][]} all unique triplets summing to 0
 */
export function threeSum(nums) {
  // your implementation here
}

/**
 * Median of Two Sorted Arrays (Hard)
 *
 * Two regional offices each maintain a sorted salary list. HR needs the
 * median salary across both offices without merging the full lists.
 * Given two sorted arrays, return the median of the combined data.
 *
 * Pattern: Binary search on partition of the smaller array,
 *          O(log(min(m, n))) time, O(1) space
 *
 * @param {number[]} nums1
 * @param {number[]} nums2
 * @return {number}
 */
export function findMedianSortedArrays(nums1, nums2) {
  // your implementation here
}

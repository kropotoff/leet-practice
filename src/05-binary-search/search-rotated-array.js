/**
 * Search in Rotated Sorted Array (Medium)
 *
 * An employee directory was shifted after a system migration (entries
 * rotated). Given a rotated sorted array of distinct values and a target
 * ID, find the index of the target or return -1.
 *
 * Pattern: Modified binary search — check which half is sorted, O(log n)
 *
 * @param {number[]} nums
 * @param {number} target
 * @return {number} index or -1
 */
export function searchRotated(nums, target) {
  // your implementation here
}

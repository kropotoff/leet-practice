/**
 * Binary Search (Easy)
 *
 * A warehouse inventory system needs to quickly locate a product by its
 * sorted SKU number. Given a sorted (ascending) integer array and a
 * target, return the index of the target or -1 if not found.
 *
 * Pattern: Standard binary search, O(log n) time, O(1) space
 *
 * @param {number[]} nums - sorted ascending
 * @param {number} target
 * @return {number} index or -1
 */
export function search(nums, target) {
  // your implementation here
}

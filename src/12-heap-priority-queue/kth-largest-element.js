/**
 * MinHeap — a standard min-heap (priority queue).
 *
 * You need to implement this data structure to solve the problem below.
 * Operations: insert(val), extract() (remove + return min), peek(), size()
 */
export class MinHeap {
  constructor() {
    // your initialization here
  }

  size() {
    // your implementation here
  }

  peek() {
    // your implementation here
  }

  insert(val) {
    // your implementation here
  }

  extract() {
    // your implementation here
  }
}

/**
 * Kth Largest Element in an Array (Medium)
 *
 * A leaderboard system needs to find the kth-highest player score from
 * a list of all scores. Given an unsorted array and k, return the kth
 * largest element.
 *
 * Pattern: Min-heap of size k, O(n log k) time
 *
 * @param {number[]} nums
 * @param {number} k
 * @return {number}
 */
export function findKthLargest(nums, k) {
  // your implementation here
}

/**
 * Number of Islands (Medium)
 *
 * A real-estate mapping tool counts distinct land parcels in a grid
 * survey where '1' is land and '0' is water. Adjacent land cells
 * (horizontal/vertical) belong to the same parcel. Return the total
 * number of separate parcels.
 *
 * Pattern: DFS/BFS flood fill on 2D grid, O(m * n) time
 *
 * @param {string[][]} grid
 * @return {number}
 */
export function numIslands(grid) {
  // your implementation here
}

/**
 * Course Schedule (Medium)
 *
 * An onboarding system checks whether all training modules can be
 * completed given prerequisite dependencies. prerequisites[i] = [a, b]
 * means module b must be finished before module a. Return true if all
 * modules can be completed (i.e. no circular dependencies).
 *
 * Pattern: Topological sort / cycle detection (Kahn's BFS), O(V + E)
 *
 * @param {number} numCourses
 * @param {number[][]} prerequisites - [course, prereq] pairs
 * @return {boolean}
 */
export function canFinish(numCourses, prerequisites) {
  // your implementation here
}

/**
 * Maximum Subarray (Medium)
 *
 * A sales analytics tool finds the contiguous period with the highest
 * cumulative revenue. Given an array of daily revenues (can be negative),
 * return the maximum subarray sum.
 *
 * Pattern: Kadane's algorithm (greedy), O(n) time, O(1) space
 *
 * @param {number[]} nums
 * @return {number}
 */
export function maxSubArray(nums) {
  // your implementation here
}

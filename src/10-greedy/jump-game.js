/**
 * Jump Game (Medium)
 *
 * A project planner checks if a team can reach the final milestone,
 * where each milestone i allows skipping ahead by up to nums[i] steps.
 * Return true if the last index is reachable from index 0.
 *
 * Pattern: Greedy — track max reachable index, O(n) time
 *
 * @param {number[]} nums
 * @return {boolean}
 */
export function canJump(nums) {
  // your implementation here
}

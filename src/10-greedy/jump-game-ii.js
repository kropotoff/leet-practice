/**
 * Jump Game II (Medium)
 *
 * Same setup as Jump Game, but now find the minimum number of jumps
 * needed to reach the final milestone. You can assume the last index
 * is always reachable.
 *
 * Pattern: Greedy BFS-like range tracking, O(n) time
 *
 * @param {number[]} nums
 * @return {number}
 */
export function jump(nums) {
  // your implementation here
}
